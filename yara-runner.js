self.onmessage=async({data:{buffer,rules}})=>{let ptr=0,m;try{const {default:create}=await import('./vendor/yara.js');m=await create();ptr=m._malloc(buffer.byteLength);if(!ptr)throw Error('Недостаточно WASM-памяти');m.HEAPU8.set(new Uint8Array(buffer),ptr);const code=m.ccall('aw_scan','number',['number','number','string'],[ptr,buffer.byteLength,rules]);if(code<0)throw Error(code===-1?'Ошибка компиляции правил':'Ошибка YARA '+code);postMessage({matches:m.UTF8ToString(m._aw_result()).trim().split('\n').filter(Boolean),engine:'YARA 4.5.8'})}catch(e){postMessage({error:e.message})}finally{if(ptr)m._free(ptr)}};

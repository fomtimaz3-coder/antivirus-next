let lastRequest=0,chain=Promise.resolve();
const TTL=6*3600000;
function cache(){return new Promise((resolve,reject)=>{const r=indexedDB.open('aw-reputation',1);r.onupgradeneeded=()=>r.result.createObjectStore('hashes',{keyPath:'sha256'});r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)})}
async function cached(hash,value){const db=await cache();try{return await new Promise((resolve,reject)=>{const tx=db.transaction('hashes',value?'readwrite':'readonly'),s=tx.objectStore('hashes'),r=value?s.put(value):s.get(hash);let result;r.onsuccess=()=>result=r.result;tx.oncomplete=()=>resolve(result);tx.onerror=()=>reject(tx.error)})}finally{db.close()}}
export function reputation(hash,key,active=()=>true){const task=async()=>{if(!active())return {status:'disabled'};const old=await cached(hash).catch(()=>null);if(old&&Date.now()-old.checkedAt<TTL)return {...old,cached:true};const wait=Math.max(0,16000-(Date.now()-lastRequest));if(wait)await new Promise(r=>setTimeout(r,wait));if(!active())return {status:'disabled'};lastRequest=Date.now();try{const r=await fetch('./api/reputation',{method:'POST',headers:{'Content-Type':'application/json','x-vt-key':key},body:JSON.stringify({sha256:hash}),signal:AbortSignal.timeout(15000)});if(!r.ok)return {status:'unavailable',reason:r.status===429?'Лимит VirusTotal':r.status===401?'Проверьте API-ключ':'Сервис недоступен'};const v=await r.json();if(v.sha256!==hash)return {status:'unavailable',reason:'Некорректный ответ'};await cached(hash,v).catch(()=>{});return v}catch{return {status:'unavailable',reason:'Нет связи с сервисом'}}};const next=chain.then(task,task);chain=next.catch(()=>{});return next}

export async function clearReputation(){const db=await cache();try{await new Promise((resolve,reject)=>{const tx=db.transaction('hashes','readwrite');tx.objectStore('hashes').clear();tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error)})}finally{db.close()}}
